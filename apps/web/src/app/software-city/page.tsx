'use client';

import React from 'react';
import { MetaverseWorkspaceContainer } from '@/components/metaverse/metaverse-workspace-container';

export default function SoftwareCityPage() {
  return (
    <div className="-m-6 h-[calc(100vh-4rem)] overflow-hidden">
      <MetaverseWorkspaceContainer />
    </div>
  );
}
