# CodeAtlas Infrastructure Graph Specification

Graph representation of physical and virtual infrastructure relationships: `Service` $\rightarrow$ `Container` $\rightarrow$ `Cluster` $\rightarrow$ `Network` $\rightarrow$ `Database` $\rightarrow$ `Storage`.
