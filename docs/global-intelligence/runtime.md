# CodeAtlas Runtime Service Topology Specification

Live runtime service call graph mapping observed HTTP, gRPC, and message queue communication paths against static AST declarations to identify architecture drift.
