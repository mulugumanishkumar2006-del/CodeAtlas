# CodeAtlas Policy Precedence & Policy Simulation Engine Specification

Policy Precedence rule (`Global` $\rightarrow$ `Organization` $\rightarrow$ `Workspace` $\rightarrow$ `Environment` $\rightarrow$ `Repository` $\rightarrow$ `Service` $\rightarrow$ `User` $\rightarrow$ `Agent`) where the most restrictive policy wins by default.
